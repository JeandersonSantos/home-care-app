import { Stack, Typography } from "@mui/material";

const ScheduleMedicalCare = () => {
  return (
    <Stack>
      <Typography variant="h4">Agendar Atendimento</Typography>
    </Stack>
  );
};
export default ScheduleMedicalCare;
